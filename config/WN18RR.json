20-07-06 18:59:01
Lambda, 0.05
action_embed_size, 50
batch_size, 256
beta, 0.05
config_dir, ../config
data_dir, ../datasets
dataset, WN18RR
decay_batch, 100
decay_rate, 0.9
decay_weight, 0.02
entity_embed_size, 50
gamma, 1
grad_clip_norm, 5
learning_rate, 0.001
log_dir, ../logs
max_out, 100
max_step_length, 3
mlp_hidden_size, 100
model_dir, ../saved_models
num_entity, 40944
num_relation, 25
relation_embed_size, 50
state_embed_size, 50
test_times, 100
train_batch, 20
train_times, 20
use_cuda, True
use_entity_embed, False

